#include <conio.h>
#include <iostream>
#include <math.h>
using namespace std;
int main()
{
	int cont1=1, num=1,n;
	for (int i = 0; i < 10; i++)
	{
		for (int a = 0; a < 3; a++)
		{
			n = pow(num, cont1);
			cout << n << "    ";
			cont1++;
		}
		num++;
		cout << endl;
		cont1 = 1;
	}

	_getch();
}
